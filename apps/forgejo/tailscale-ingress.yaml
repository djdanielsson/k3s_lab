apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: forgejo-ts
  namespace: forgejo
  labels:
    app.kubernetes.io/instance: tailscale-ingress
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: forgejo
      port:
        number: 3000
