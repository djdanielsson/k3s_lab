apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netalertx-data
  namespace: netalertx
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
