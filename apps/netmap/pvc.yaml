apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netmap-data
  namespace: netmap
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
