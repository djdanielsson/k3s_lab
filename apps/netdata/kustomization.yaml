# Netdata - lightweight real-time node monitoring (DaemonSet, hostNetwork).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - daemonset.yaml
  - service.yaml
