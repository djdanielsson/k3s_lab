apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: pantrywise
  labels:
    app: pantrywise-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pantrywise-postgres
  template:
    metadata:
      labels:
        app: pantrywise-postgres
    spec:
      containers:
        - name: postgres
          image: docker.io/library/postgres:17-alpine
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pantrywise-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pantrywise-secrets
                  key: postgres-password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: pantrywise-secrets
                  key: postgres-db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pantrywise-postgres-pvc