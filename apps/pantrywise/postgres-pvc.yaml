apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pantrywise-postgres-pvc
  namespace: pantrywise
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi