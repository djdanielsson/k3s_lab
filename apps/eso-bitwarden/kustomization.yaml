# ESO + Vaultwarden (bitwarden-cli sidecar + webhook provider). Mirrors ocp_lab.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - clustersecretstore.yaml
