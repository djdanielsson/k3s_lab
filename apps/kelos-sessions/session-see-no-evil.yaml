# Session: see-no-evil (PUBLIC, opencode-go/muse-spark-1.3-contributor)
apiVersion: kelos.dev/v1alpha2
kind: Session
metadata:
  name: see-no-evil
  namespace: kelos
  labels:
    app.kubernetes.io/part-of: kelos
    kelos.dev/repo: see-no-evil
spec:
  initialPrompt: |
    Ready to work on see-no-evil. Ask what you should do or give me a task.
  volumeClaimTemplate:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 10Gi
  worker:
    type: opencode
    model: opencode-go/muse-spark-1.3-contributor
    credentials:
      type: api-key
      secretRef:
        name: opencode-credentials
    workspaceRef:
      name: workspace-see-no-evil
