apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - tailscale-ingress.yaml
  # NOTE: Traefik ingress for the RustFS console lives in ../ingress (disabled).
