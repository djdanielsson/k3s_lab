# Dedicated ServiceAccount for the in-cluster Hermes agent. The pod mounts its
# token and kubectl auto-detects the in-cluster config (no kubeconfig file).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hermes
  namespace: hermes
  labels:
    app: hermes