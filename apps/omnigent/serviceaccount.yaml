# The identity the server Pod runs as. sandbox-rbac.yaml binds it, in the
# runner namespace, to the rights the sandbox launcher needs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: omnigent
  namespace: omnigent
  labels:
    app: omnigent
