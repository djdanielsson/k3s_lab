apiVersion: v1
kind: Service
metadata:
  name: omnigent-postgres
  namespace: omnigent
spec:
  type: ClusterIP
  selector:
    app: omnigent-postgres
  ports:
    - port: 5432
      targetPort: pg
      protocol: TCP
