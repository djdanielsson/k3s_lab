apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omnigent-pg-data
  namespace: omnigent
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
