# cert-manager issuers — DISABLED by default.
# Enable by uncommenting the resource line below AND the docs in clusterissuer.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# resources:
#   - clusterissuer.yaml
